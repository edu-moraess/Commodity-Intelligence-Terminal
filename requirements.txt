# Commodity Intelligence Terminal — Dependências
# Instalar com: pip install -r requirements.txt

streamlit>=1.38,<2.0
plotly>=5.22,<6.0
pandas>=2.2,<3.0
numpy>=1.26,<2.0
scipy>=1.13,<2.0
scikit-learn>=1.5,<2.0
yfinance>=0.2.40,<0.3
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0

# --- Opcionais (habilitados no roadmap de expansão — ver README) ---
# polars>=1.5           # engine alternativa de dataframe de alta performance
# duckdb>=1.0           # OLAP local para consultas sobre grandes painéis
# statsmodels>=0.14      # VAR / VECM / cointegração
# arch>=7.0              # EGARCH / DCC-GARCH mais completos que o GARCH(1,1) próprio
# prophet>=1.1            # forecasting sazonal (Meta Prophet)
# xgboost>=2.1
# lightgbm>=4.5
# catboost>=1.2
# torch>=2.3              # LSTM / GRU / TFT
# mlflow>=2.16            # model registry e tracking de experimentos